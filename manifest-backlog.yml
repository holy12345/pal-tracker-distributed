applications:
- name: tracker-backlog
  path: ./applications/backlog-server/build/libs/backlog-server.jar
  host: backlog
  memory: 1G
  instances: 1
  env:
    REGISTRATION_SERVER_ENDPOINT: registration
  services:
  - tracker-backlog-database
